function solution(A, B) {
  return (B+B).indexOf(A)
  // let count = 0;
  // let a = A
  // for (let i=0; i<a.length; i++) {
  //     if(a===B) return count
  //     a = a.slice(-1) + a.slice(0, -1)
  //     count++
  // }
  // return -1
}